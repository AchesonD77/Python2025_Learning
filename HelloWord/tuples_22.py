# Tuples are similar to list
# we can not modify them, we cannot add new items, we can not remove existing items.
# tuples are immutable

# list uses []
numbers = [1, 2, 3]
# tuple uses ()
numbers = (1, 2, 3)
"""
numbers.count()
numbers.index()
"""
print(numbers[0])
# we use tuple to define a unchange items
